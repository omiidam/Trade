name: Android Release

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build web application
        run: npx vite build

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Add Android platform
        run: npx cap add android

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Generate Release Keystore
        run: |
          rm -f android/app/veltrix-release.keystore
          keytool -genkeypair -v \
            -keystore android/app/veltrix-release.keystore \
            -alias veltrix \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storepass veltrix123 \
            -keypass veltrix123 \
            -dname "CN=Veltrix Dashboard, OU=Development, O=Veltrix, L=San Francisco, ST=CA, C=US"

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Release APK
        working-directory: android
        run: ./gradlew assembleRelease

      - name: Verify APK exists
        run: |
          APK_PATH="android/app/build/outputs/apk/release/app-release.apk"
          if [ ! -f "$APK_PATH" ]; then
            echo "ERROR: Release APK not found at $APK_PATH"
            exit 1
          fi
          echo "✅ Release APK found at $APK_PATH"
          ls -lh "$APK_PATH"

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: veltrix-release-apk
          path: android/app/build/outputs/apk/release/app-release.apk
          retention-days: 30
